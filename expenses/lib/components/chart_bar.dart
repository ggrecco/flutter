import 'package:flutter/material.dart';

class ChartBart extends StatelessWidget {

  final String label;
  final double value;
  

  ChartBart(this.label, this.value);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}